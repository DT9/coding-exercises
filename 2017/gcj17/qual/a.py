def f(s,k):
    pass
def flip(p,s,k,c):
    if p.count('+') == len(p):
       return c
    if p[s] == 
    # for p in range(a):
    #     if (p == '-'):
            

tc = int(input())
for t,line in enumerate(range(tc)):
    a,b = input().split()
    b = int(b)
    ans = ""
    cnt = 0
    cnt = flip(p,0,b)
    print("Case #{}: {}".format(t+1,cnt))
